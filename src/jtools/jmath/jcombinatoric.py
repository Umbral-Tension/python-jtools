





def get_index_permutations(item, n: int) -> []:
    """Get a list of tuples where each tuple contains one possible n-sized permutation of the
    set of all indices in "item". 

    The index tuples can then be used to obtain the set of all n-sized permutations of the actual
    values in any indexed object of length len(item). 
    
    Args:
        item (indexed): an indexed object.
        n (int): the length of each permutation to be found 

    Returns:
        (list): A list of permutations
    """ 
    pass





